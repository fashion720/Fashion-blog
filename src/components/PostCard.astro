---
interface Props {
  title: string;
  slug: string;
  description: string;
  featuredImage?: string;
  author?: string;
  publishedDate: string;
  categories?: string[];
  tags?: string[];
}

const {
  title,
  slug,
  description,
  featuredImage,
  author,
  publishedDate,
  categories = [],
  tags = [],
} = Astro.props;

// Image path fix: agar already /images/ se shuru ho toh dobara mat lagao
function resolveImage(img?: string) {
  if (!img) return '/images/placeholder.jpg';
  if (img.startsWith('/')) return img;
  return `/images/posts/${img}`;
}

const imgSrc = resolveImage(featuredImage);

const formattedDate = new Date(publishedDate).toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});
---

<article class="post-card group w-full">
  <a href={`/posts/${slug}`} class="block no-underline">
    <!-- Image Component (Stripped from absolute overlays for a cleaner look) -->
    <div class="overflow-hidden aspect-[3/4] bg-neutral-100 mb-3">
      <img
        src={imgSrc}
        alt={title}
        width="600"
        height="800"
        loading="lazy"
        decoding="async"
        class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-102"
      />
    </div>

    <!-- Meta & Typography Content -->
    <div class="text-left px-1">
      <!-- Category Tag (Elegant muted link format directly below image) -->
      {categories.length > 0 && (
        <span class="block text-[11px] font-bold uppercase tracking-widest text-[#bba15c] mb-1.5">
          {categories[0].replace(/-/g, ' ')}
        </span>
      )}

      <!-- Title (Clean Editorial Spacing) -->
      <h3 class="text-lg font-medium tracking-tight text-neutral-900 mb-1.5 group-hover:text-neutral-600 transition-colors duration-200 leading-tight">
        {title}
      </h3>

      <!-- Description Excerpt Line -->
      <p class="text-[13px] font-normal text-neutral-500 leading-relaxed line-clamp-2 mb-2">
        {description}
      </p>

      <!-- Date/Author footer wrapper -->
      <div class="text-[10px] uppercase tracking-widest text-neutral-400">
        <time datetime={publishedDate}>{formattedDate}</time>
      </div>
    </div>
  </a>
</article>

<style>
  /* Clean Webkit line-clamping helper safely built for Astro output */
  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
</style>
